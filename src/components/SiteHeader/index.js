import { SkipLink } from '@octopusthink/nautilus';

import React from 'react';

const SiteHeader = () => {
  return (
    <header>
      <SkipLink />
    </header>
  );
};

export default SiteHeader;
